# react-gemini

Estructura base del proyecto:

- `frontend/`: app React con Vite
- `backend/`: API Node con Express

## Puesta en marcha

Frontend:

```bash
cd frontend
npm install
npm run dev
```

Backend:

```bash
cd backend
npm install
cp .env.example .env
npm run dev
```
